import { motion } from 'framer-motion'
import { features } from '../data/siteData'
import { SectionHeading, reveal } from './ui'

export function Features() { return <section id="features" className="scroll-mt-24 px-5 py-20 sm:py-28"><div className="mx-auto max-w-7xl"><div className="grid gap-10 lg:grid-cols-[.75fr_1.25fr]"><SectionHeading align="left" eyebrow="BUILT TO OUTRUN" title={<>Premium infrastructure.<br /><span className="text-gradient">Zero compromises.</span></>}>You bring the ideas. We make sure the performance never holds them back.</SectionHeading><motion.div className="grid gap-3 sm:grid-cols-2" initial="hidden" whileInView="visible" viewport={{ once: true, margin: '-50px' }} variants={{ visible: { transition: { staggerChildren: .05 } } }}>{features.map(([name, description, Icon]) => <motion.div key={name} variants={reveal} whileHover={{ y: -4 }} className="glass-card group rounded-xl p-5 transition hover:border-cyan-300/25 hover:bg-cyan-300/[.035]"><Icon size={20} className="text-violet-300 transition group-hover:text-cyan-300" /><h3 className="mt-5 text-sm font-bold text-white">{name}</h3><p className="mt-2 text-xs leading-5 text-zinc-500">{description}</p></motion.div>)}</motion.div></div></div></section> }
