import { motion } from 'framer-motion'
import { Star } from 'lucide-react'
import { stats, testimonials } from '../data/siteData'
import { SectionHeading, reveal } from './ui'

export function Testimonials() { return <section id="support" className="scroll-mt-24 px-5 py-20 sm:py-28"><div className="mx-auto max-w-7xl"><div className="grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><SectionHeading align="left" eyebrow="LOVED BY BUILDERS" title={<>Made for the <span className="text-gradient">people</span> behind the worlds.</>}>From their first server to their next big network, creators count on Crazy Cloud.</SectionHeading><motion.div className="grid gap-3 sm:grid-cols-3" initial="hidden" whileInView="visible" viewport={{ once: true }} variants={{ visible: { transition: { staggerChildren: .1 } } }}>{testimonials.map(t => <motion.article key={t.name} variants={reveal} whileHover={{ y: -5 }} className="rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><div className="flex items-center gap-3"><span className={`grid h-9 w-9 place-items-center rounded-full bg-gradient-to-br ${t.color} text-sm font-bold text-white`}>{t.initial}</span><div><p className="text-sm font-bold text-white">{t.name}</p><p className="text-[10px] text-zinc-500">{t.role}</p></div></div><div className="mt-5 flex gap-1 text-cyan-300">{Array.from({ length: 5 }).map((_, i) => <Star key={i} size={12} fill="currentColor" />)}</div><p className="mt-4 text-sm leading-6 text-zinc-400">“{t.review}”</p></motion.article>)}</motion.div></div><div className="mt-16 grid grid-cols-2 gap-px overflow-hidden rounded-2xl border border-white/[.08] bg-white/[.08] md:grid-cols-4">{stats.map(([value, label]) => <div key={label} className="bg-[#101015] px-5 py-7 text-center"><p className="font-display text-3xl font-bold text-white sm:text-4xl">{value}</p><p className="mt-2 text-xs text-zinc-500">{label}</p></div>)}</div></div></section> }
